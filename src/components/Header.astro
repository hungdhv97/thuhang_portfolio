---
import { profile } from "../data/content";

const navLinks = [
  { href: "#about", label: "Về tôi" },
  { href: "#work", label: "Công việc" },
  { href: "#experience", label: "Kinh nghiệm" },
  { href: "#skills", label: "Kỹ năng" },
  { href: "#contact", label: "Liên hệ" },
];
---

<header
  class="site-header sticky top-0 z-40 w-full border-b border-[var(--color-rule)] bg-[color-mix(in_oklch,var(--color-paper)_86%,transparent)] backdrop-blur-md"
>
  <nav
    class="letter flex h-14 items-center justify-between gap-4 py-3.5 md:h-16 md:py-4"
    aria-label="Primary"
  >
    <a
      href="#top"
      class="font-display text-xl tracking-tight text-[var(--color-ink)] no-underline"
    >
      {profile.shortName}
    </a>

    <div class="flex items-center gap-4 sm:gap-6">
      <div class="hidden items-center gap-5 md:flex md:gap-7">
        {
          navLinks.map((link) => (
            <a href={link.href} class="nav-a label">
              {link.label}
            </a>
          ))
        }
      </div>

      <button
        type="button"
        class="theme-toggle cursor-pointer border-0 bg-transparent p-0 text-[var(--color-ink-soft)] transition-colors hover:text-[var(--color-accent)] active:scale-[0.97]"
        aria-label="Chuyển sang giao diện tối"
      >
        <span class="theme-icon theme-icon-moon" aria-hidden="true">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.75"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <path d="M21 14.5A8.5 8.5 0 0 1 9.5 3 7 7 0 1 0 21 14.5z"></path>
          </svg>
        </span>
        <span class="theme-icon theme-icon-sun" aria-hidden="true">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="1.75"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <circle cx="12" cy="12" r="4"></circle>
            <path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.41-1.41M17.66 6.34l1.41-1.41"></path>
          </svg>
        </span>
      </button>

      <button
        type="button"
        id="menu-toggle"
        class="menu-toggle relative flex h-10 w-10 items-center justify-center border-0 bg-transparent p-0 md:hidden"
        aria-expanded="false"
        aria-controls="mobile-drawer"
        aria-label="Mở menu"
      >
        <span class="menu-toggle-bars" aria-hidden="true">
          <span class="menu-bar menu-bar-1"></span>
          <span class="menu-bar menu-bar-2"></span>
        </span>
      </button>
    </div>
  </nav>
</header>

<div
  id="menu-backdrop"
  class="menu-backdrop"
  hidden
  aria-hidden="true"
></div>

<aside
  id="mobile-drawer"
  class="mobile-drawer"
  hidden
  aria-hidden="true"
  aria-label="Menu điều hướng"
>
  <div class="mobile-drawer-inner">
    <p class="label mb-8 text-[var(--color-ink-soft)]">{profile.role}</p>
    <nav class="flex flex-col gap-1" aria-label="Mobile">
      {
        navLinks.map((link, i) => (
          <a
            href={link.href}
            class="drawer-link font-display text-[2.2rem] tracking-[-0.02em] text-[var(--color-ink)] no-underline"
            style={`--i: ${i}`}
          >
            {link.label}
          </a>
        ))
      }
    </nav>
  </div>
</aside>

<script>
  function initMobileMenu() {
    const toggle = document.getElementById("menu-toggle");
    const drawer = document.getElementById("mobile-drawer");
    const backdrop = document.getElementById("menu-backdrop");
    if (!toggle || !drawer || !backdrop) return;

    const links = Array.from(drawer.querySelectorAll("a"));
    const reduce = window.matchMedia("(prefers-reduced-motion: reduce)");
    let open = false;
    /** @type {HTMLElement | null} */
    let lastFocus = null;

    const setOpen = (next: boolean) => {
      open = next;
      document.documentElement.classList.toggle("menu-open", next);
      toggle.setAttribute("aria-expanded", String(next));
      toggle.setAttribute("aria-label", next ? "Đóng menu" : "Mở menu");
      drawer.hidden = false;
      backdrop.hidden = false;
      drawer.setAttribute("aria-hidden", String(!next));
      backdrop.setAttribute("aria-hidden", String(!next));

      if (next) {
        lastFocus = document.activeElement as HTMLElement;
        requestAnimationFrame(() => {
          drawer.classList.add("is-open");
          backdrop.classList.add("is-open");
          links[0]?.focus();
        });
      } else {
        drawer.classList.remove("is-open");
        backdrop.classList.remove("is-open");
        const finish = () => {
          if (!open) {
            drawer.hidden = true;
            backdrop.hidden = true;
          }
        };
        if (reduce.matches) finish();
        else setTimeout(finish, 380);
        lastFocus?.focus();
      }
    };

    toggle.addEventListener("click", () => setOpen(!open));
    backdrop.addEventListener("click", () => setOpen(false));
    links.forEach((link) => link.addEventListener("click", () => setOpen(false)));

    document.addEventListener("keydown", (e) => {
      if (!open) return;
      if (e.key === "Escape") {
        e.preventDefault();
        setOpen(false);
        return;
      }
      if (e.key !== "Tab" || links.length === 0) return;
      const focusables = [toggle, ...links];
      const first = focusables[0];
      const last = focusables[focusables.length - 1];
      const active = document.activeElement;
      if (e.shiftKey && active === first) {
        e.preventDefault();
        last.focus();
      } else if (!e.shiftKey && active === last) {
        e.preventDefault();
        first.focus();
      }
    });

    const mq = window.matchMedia("(min-width: 768px)");
    const onBreakpoint = () => {
      if (mq.matches && open) setOpen(false);
    };
    mq.addEventListener("change", onBreakpoint);
  }

  initMobileMenu();
</script>
